{{render 'state'}}
<div class="resize horizontal"></div>
<section class="workspace">
  {{render 'canvas'}}
  <div class="resize vertical"></div>
  {{render 'details'}}
  <div class="resize vertical"></div>
  {{render 'stepper'}}
</section>
<div class="resize horizontal"></div>
{{render 'router'}}
